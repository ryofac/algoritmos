# Entrada
base = float('Digite o valor base de um retângulo: ')
altura = float(input('Digite o valor da altura desse mesmo retângulo: '))

# Processamento
area =(base * altura)

# Saída
print(f'A área desse retângulo é de {area}')